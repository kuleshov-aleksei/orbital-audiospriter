services:
  audiospriter:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: ${VERSION:-dev}
    image: orbital-audiospriter:${VERSION:-dev}
    restart: unless-stopped
    ports:
      - "8026:8000"